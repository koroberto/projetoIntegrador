<!-- Início da coluna 3 - Resultados -->
<div
  class="col-sm-3 p-0 m-0 justify-content-center">
  
  <div>  
  <%# console.log(times) %>
  <% let meuTime = times.find((time) => time.jogadores_id == jogador.id) %>
  <% let oculta; let escudoMeuTime; %>
  <% if(meuTime == undefined) { escudoMeuTime = ""; oculta="d-none"; meuTime = { nome: ""} ;} else {  oculta="" ; escudoMeuTime = meuTime.MidiasTimes[0].path}  %>
  
  <div class="container-fluid p-2 border-penochao border-penochao-mobile rounded-top-penochao rounded-bottom-penochao mb-3 pb-3 px-4 <%= oculta %>">
    <a href="/time/exibir/<%= meuTime.id %>">
    <div class="mt-2">
      <% jogador.nome = jogador.nome.split(" ") %>
      <h5 class="text-center" style="color:black"><%= jogador.nome[0] %> joga no time</h5>
    </div>
    <!--Divi das escudo do meu Time-->
    <div class="row mt-3">
        <div class="col d-flex justify-content-center">
          <img src="/images/midias/<%= escudoMeuTime %>" width="50%">
        </div>
    </div>
    <div class="row mt-2">
      <div class="col d-flex justify-content-center">
        <a href="/times" style="color:black"> <%= meuTime.nome %> </a>
      </div>
    </div>
  </a>
  </div>
  
<!--Div do grado dos jogadores destaque-->
  <div class="container-fluid p-2 border-penochao border-penochao-mobile rounded-top-penochao rounded-bottom-penochao mb-3 pb-3 px-4">
    <div class="mt-2">
      <h5 class="text-center">Seguidores</h5>
    </div>
    <!--Divi das fotos dos Seguidores-->
    <div class="row mt-3">
      <% for(let x of jogadores) { %>
        <% if (x.id!=jogador.id){ %>
      <div class="col-sm-4 justify-content-center mb-2">
        <div class="row justify-content-center">
          <img src="/images/midias/<%=x.path%>"  width="50vw" class="rounded-circle foto-perfil-jogadores">
        </div>

        <div class="row justify-content-center">
          <a href="/perfilJogador/<%=x.id%>">
            <% nomeJogador = x.nome.split(" ") %>
            <%= nomeJogador[0] %>
          </a>
        </div>
      </div>
      <%}%>
      <% } %>

      <!--

      <div class="col-sm-4 justify-content-center">
        <div class="row justify-content-center">
          <img src="/images/<%=jogador.path%>"  width="50vw" class="rounded-circle">
        </div>

        <div class="row justify-content-center">
          <%=jogador.nome%>
        </div>
      </div>

      <div class="col-sm-4 justify-content-center">
        <div class="row justify-content-center">
          <img src="/images/<%=jogador.path%>"  width="50vw" class="rounded-circle">
        </div>

        <div class="row justify-content-center">
          <%=jogador.nome%>
        </div>
      </div>
    </div>

    <div class="row mt-3">
      <div class="col-sm-4 justify-content-center">
        <div class="row justify-content-center">
          <img src="/images/<%=jogador.path%>"  width="50vw" class="rounded-circle">
        </div>

        <div class="row justify-content-center">
          <%=jogador.nome%>
        </div>
      </div>

      <div class="col-sm-4 justify-content-center">
        <div class="row justify-content-center">
          <img src="/images/<%=jogador.path%>"  width="50vw" class="rounded-circle">
        </div>

        <div class="row justify-content-center">
          <%=jogador.nome%>
        </div>
      </div>

      <div class="col-sm-4 justify-content-center">
        <div class="row justify-content-center">
          <img src="/images/<%=jogador.path%>"  width="50vw" class="rounded-circle">
        </div>

        <div class="row justify-content-center">
          <%=jogador.nome%>
        </div>
      </div>
      -->
    </div>
  </div>

<!--Div do grado dos jogadores destaque-->
<div class="container-fluid p-2 border-penochao border-penochao-mobile rounded-top-penochao rounded-bottom-penochao mb-3 pb-3 px-4">
  <div class="mt-2">
    <h5 class="text-center">Times</h5>
  </div>
  <!--Divi das fotos dos jogadores-->
  <div class="row mt-3">
    <% console.log(times) %>
    <% if(times != undefined) { for(let x of times) { let file = x.MidiasTimes[0].path; %>
    <%# for(let x of jogadores) { %>
    <%# if (x.id!=jogador.id){ %>
    <div class="col-sm-4 justify-content-center mb-2">
      <div class="row justify-content-center">
        <img src="/images/midias/<%= file %>"  width="50vw" class="rounded-circle foto-perfil-jogadores">
      </div>

      <div class="row justify-content-center">
        <a href="/time/exibir/<%=x.id%>">
          <% nomeTime = x.nome.split(" ") %>
          <%= nomeTime[0] %>
        </a>
      </div>
    </div>
    <%}%>
  <% }%>
  </div>
</div>


  <!--Div dos times-->
  <div class="container-fluid p-2 border-penochao border-penochao-mobile rounded-top-penochao rounded-bottom-penochao ">
    <div class="mt-2">
      <h5 class="text-center">
        <a href="/times" style="color:black">Times</a> </h5>
    </div>

  </div>





  </div>
</div>
<!-- Fim da coluna 3 - Resultados -->